# Secrets and credentials
.env
*.env
acme.json
*_token*
*_secret*
*password*
*.key
*.pem
*.crt

# Sensitive config overrides
docker-compose.override.yml
secrets/

# Editors
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.*
terraform/.terraform.lock.hcl
terraform/terraform.tfvars

# Claude
.claude/

# Gemini
.gemini/
GEMINI.md

# Codex
.codex
.codex/

# Python bytecode (scripts/ is stdlib-only, but py_compile leaves these)
__pycache__/
*.pyc

/.claudeignore
/.graphifyignore

# BEGIN never4ga agent instruction files (ADR-0025)
# These are generated pointers, not content. What they used to carry
# lives in the never-knowledge vault and reaches a session through the
# Context Pack.
AGENTS.md
CLAUDE.md
GEMINI.md
# END never4ga agent instruction files
