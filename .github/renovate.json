{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "America/New_York",
  "labels": ["dependencies"],
  "prConcurrentLimit": 6,
  "prHourlyLimit": 0,
  "dependencyDashboardTitle": "Renovate — dependency dashboard (FORGE-81)",
  "ansible-galaxy": {
    "description": "requirements.yml collections are intentionally unpinned; nothing to bump until versions are added.",
    "enabled": true
  },
  "docker-compose": { "enabled": false },
  "dockerfile": { "enabled": false },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Container images in compose files, Jinja (.j2) templates, and Ansible *_image vars. Handles the FORGE-70 tag@sha256 form, tag-only images, and digest-only (no-tag) images. Skips values containing Jinja {{ }} or ${ } (unresolvable tags). IMAGE VALUES MUST BE UNQUOTED: the regex is unanchored so it also matches `<something>_image: `, and the match would include an opening quote but not the closing one, so autoReplaceStringTemplate would emit a dangling quote and corrupt the YAML. Quote support was therefore removed from matchStrings on purpose -- a quoted value is now silently unmanaged (safe) rather than rewritten into broken YAML (unsafe). autoReplaceStringTemplate is what lets Renovate PIN a digest onto a tag-only image; without it the pin branch failed on every run for 3+ weeks with 'Digest is not updated / update failure' because currentDigest never participated in the match.",
      "managerFilePatterns": [
        "/^ansible/roles/.+\\.ya?ml(\\.j2)?$/",
        "/^docker/.+\\.ya?ml$/"
      ],
      "matchStrings": [
        "image:\\s+(?<depName>[^\\s:@\"'${}]+)(?::(?<currentValue>[^\\s@\"'${}]+))?(?:@(?<currentDigest>sha256:[a-f0-9]{64}))?"
      ],
      "autoReplaceStringTemplate": "image: {{{depName}}}{{#if newValue}}:{{{newValue}}}{{/if}}{{#if newDigest}}@{{{newDigest}}}{{/if}}",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "The Renovate ENGINE version passed to renovatebot/github-action as renovate-version. The action's own default is a floating major tag re-pulled every run, so without this pin the tool that rewrites this repo changes build-to-build and a major can arrive inside an unrelated action bump. Kept as a bare semver string (NOT an image ref) -- the pinDigests exception below exists because appending a digest here would make the input invalid.",
      "managerFilePatterns": ["/^\\.github/workflows/renovate\\.yml$/"],
      "matchStrings": [
        "renovate-version:\\s+(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ],
      "autoReplaceStringTemplate": "renovate-version: {{{newValue}}}",
      "depNameTemplate": "ghcr.io/renovatebot/renovate",
      "datasourceTemplate": "docker",
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "description": "Pin digests for every managed container image — aligns updates to the FORGE-70 tag@sha256 standard (adds a digest to tag-only images on bump).",
      "matchDatasources": ["docker"],
      "pinDigests": true
    },
    {
      "description": "The renovate-version input is a bare version string, not an image reference — a digest pin would emit '44.24.3@sha256:...' and the action would fail to resolve it. Must come AFTER the pinDigests rule above; later rules win.",
      "matchPackageNames": ["ghcr.io/renovatebot/renovate"],
      "pinDigests": false
    },
    {
      "description": "Coordinated app stacks — images are version-locked as a unit and updated via their own runbooks (ERPNext = frappe_docker + bench migrate; LangFuse = the v3 stack). Renovate must not bump them piecemeal.",
      "matchFileNames": [
        "ansible/roles/erpnext/**",
        "ansible/roles/services/files/langfuse-compose.yml"
      ],
      "enabled": false
    },
    {
      "description": "Chainguard free-tier images garbage-collect old digests — must stay on :latest, never digest-pinned.",
      "matchPackageNames": ["cgr.dev/chainguard/**"],
      "enabled": false
    },
    {
      "description": "Database & cache engines need a manual data-dir migration on a major bump (see the postgres-18 data-dir gotcha) — allow minor/patch/digest only.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "postgres",
        "mariadb",
        "redis",
        "valkey/valkey",
        "clickhouse/clickhouse-server"
      ],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Digest-only refreshes (same tag, re-pushed image) auto-merge — low-risk, still git-visible and not live until an Ansible deploy picks them up.",
      "matchUpdateTypes": ["digest"],
      "automerge": true
    }
  ]
}
