{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "America/New_York",
  "labels": ["dependencies"],
  "prConcurrentLimit": 6,
  "prHourlyLimit": 0,
  "dependencyDashboardTitle": "Renovate — dependency dashboard (FORGE-81)",
  "ansible-galaxy": {
    "description": "requirements.yml collections are intentionally unpinned; nothing to bump until versions are added.",
    "enabled": true
  },
  "docker-compose": { "enabled": false },
  "dockerfile": { "enabled": false },
  "customDatasources": {
    "pytorch-rocm-wheels": {
      "description": "PyTorch's ROCm wheel index is an HTML listing of .whl links, not a registry any built-in datasource understands. `format: html` turns the links into version candidates; the packageRule below strips the wheel filename down to the version. The index URL carries the ROCm build (rocm7.2), so a ROCm move is a deliberate edit of comfyui_torch_index, never an automatic bump -- ROCm 10 lives on AMD's own index and changes ComfyUI's dynamic-VRAM behaviour (see the role).",
      "defaultRegistryUrlTemplate": "https://download.pytorch.org/whl/rocm7.2/{{packageName}}/",
      "format": "html"
    }
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "The PyTorch wheels the comfyui role installs. Bare version strings in an Ansible list, so no built-in manager sees them -- the same blind spot that left ollama_version three minors behind (the manager below). The pin IS the install: roles/comfyui passes these as constraints, so a stale pin is a stale venv that nothing reports. Local versions (+rocm7.2) are part of the wheel filename, which is why this uses the custom HTML datasource rather than pypi: PyPI carries the CUDA build of the same package names, and pointing at it would propose a torch that cannot see this GPU.",
      "managerFilePatterns": ["/^ansible/roles/comfyui/defaults/main\\.ya?ml$/"],
      "matchStrings": [
        "- (?<depName>torch|torchvision|torchaudio)==(?<currentValue>\\d+\\.\\d+\\.\\d+\\+rocm[\\d.]+)"
      ],
      "autoReplaceStringTemplate": "- {{{depName}}}=={{{newValue}}}",
      "datasourceTemplate": "custom.pytorch-rocm-wheels",
      "versioningTemplate": "pep440"
    },
    {
      "customType": "regex",
      "description": "The ComfyUI release the role checks out. A bare version string like ollama_version, so no built-in manager sees it. ComfyUI ships often and a release can move the node API the pinned custom nodes call -- the role warns that ComfyUI-ToSVG imports SVG from comfy_extras.nodes_images, which upstream keeps only for backward compatibility -- so this is an update to READ before merging, not a routine bump.",
      "managerFilePatterns": ["/^ansible/roles/comfyui/defaults/main\\.ya?ml$/"],
      "matchStrings": [
        "comfyui_version:\\s+(?<currentValue>v\\d+\\.\\d+\\.\\d+)"
      ],
      "autoReplaceStringTemplate": "comfyui_version: {{{newValue}}}",
      "depNameTemplate": "comfyanonymous/ComfyUI",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "The uv version the role installs from Astral's script. Bare version string, same blind spot. comfyui_python_version is deliberately NOT watched: a Python minor bump rebuilds the venv and every wheel in it, which is a deliberate act, not a dependency update.",
      "managerFilePatterns": ["/^ansible/roles/comfyui/defaults/main\\.ya?ml$/"],
      "matchStrings": [
        "comfyui_uv_version:\\s+\"(?<currentValue>\\d+\\.\\d+\\.\\d+)\""
      ],
      "autoReplaceStringTemplate": "comfyui_uv_version: \"{{{newValue}}}\"",
      "depNameTemplate": "astral-sh/uv",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "Container images in compose files, Jinja (.j2) templates, and Ansible *_image vars. Handles the FORGE-70 tag@sha256 form, tag-only images, and digest-only (no-tag) images. Skips values containing Jinja {{ }} or ${ } (unresolvable tags). IMAGE VALUES MUST BE UNQUOTED: the regex is unanchored so it also matches `<something>_image: `, and the match would include an opening quote but not the closing one, so autoReplaceStringTemplate would emit a dangling quote and corrupt the YAML. Quote support was therefore removed from matchStrings on purpose -- a quoted value is now silently unmanaged (safe) rather than rewritten into broken YAML (unsafe). autoReplaceStringTemplate is what lets Renovate PIN a digest onto a tag-only image; without it the pin branch failed on every run for 3+ weeks with 'Digest is not updated / update failure' because currentDigest never participated in the match.",
      "managerFilePatterns": [
        "/^ansible/roles/.+\\.ya?ml(\\.j2)?$/",
        "/^docker/.+\\.ya?ml$/"
      ],
      "matchStrings": [
        "image:\\s+(?<depName>[^\\s:@\"'${}]+)(?::(?<currentValue>[^\\s@\"'${}]+))?(?:@(?<currentDigest>sha256:[a-f0-9]{64}))?"
      ],
      "autoReplaceStringTemplate": "image: {{{depName}}}{{#if newValue}}:{{{newValue}}}{{/if}}{{#if newDigest}}@{{{newDigest}}}{{/if}}",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "The Renovate ENGINE version passed to renovatebot/github-action as renovate-version. The action's own default is a floating major tag re-pulled every run, so without this pin the tool that rewrites this repo changes build-to-build and a major can arrive inside an unrelated action bump. Kept as a bare semver string (NOT an image ref) -- the pinDigests exception below exists because appending a digest here would make the input invalid.",
      "managerFilePatterns": ["/^\\.github/workflows/renovate\\.yml$/"],
      "matchStrings": [
        "renovate-version:\\s+(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ],
      "autoReplaceStringTemplate": "renovate-version: {{{newValue}}}",
      "depNameTemplate": "ghcr.io/renovatebot/renovate",
      "datasourceTemplate": "docker",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "The Ollama version pinned in the ollama role. It is a bare version string in an Ansible var, not an `image:` line, so neither the container manager above nor any built-in manager saw it -- Ollama sat three minor versions behind (0.30.7 vs 0.33.3) from 2026-06-08 to 2026-09-05 with nothing reporting it. The role comment explains why the pin is load-bearing: an empty value only installs when the binary is MISSING, so the pin IS the update mechanism and a stale pin is a silently stale install. GitHub tags carry a leading `v` the var does not, hence extractVersionTemplate.",
      "managerFilePatterns": ["/^ansible/roles/ollama/defaults/main\\.ya?ml$/"],
      "matchStrings": [
        "ollama_version:\\s+\"(?<currentValue>\\d+\\.\\d+\\.\\d+)\""
      ],
      "autoReplaceStringTemplate": "ollama_version: \"{{{newValue}}}\"",
      "depNameTemplate": "ollama/ollama",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver",
      "extractVersionTemplate": "^v(?<version>.+)$"
    }
  ],
  "packageRules": [
    {
      "description": "The custom HTML datasource yields wheel filenames; this pulls the version out of them. Also keeps the torch trio together -- torch, torchvision and torchaudio are released as a set and the role installs them as one constraint file, so a PR bumping one alone would produce a venv PyTorch itself refuses to resolve.",
      "matchDatasources": ["custom.pytorch-rocm-wheels"],
      "extractVersion": "^(?:torch|torchvision|torchaudio)-(?<version>\\d+\\.\\d+\\.\\d+\\+rocm[\\d.]+)-cp313-cp313-",
      "groupName": "pytorch rocm wheels"
    },
    {
      "description": "Pin digests for every managed container image — aligns updates to the FORGE-70 tag@sha256 standard (adds a digest to tag-only images on bump).",
      "matchDatasources": ["docker"],
      "pinDigests": true
    },
    {
      "description": "The renovate-version input is a bare version string, not an image reference — a digest pin would emit '44.24.3@sha256:...' and the action would fail to resolve it. Must come AFTER the pinDigests rule above; later rules win.",
      "matchPackageNames": ["ghcr.io/renovatebot/renovate"],
      "pinDigests": false
    },
    {
      "description": "Coordinated app stacks — images are version-locked as a unit and updated via their own runbooks (ERPNext = frappe_docker + bench migrate; LangFuse = the v3 stack). Renovate must not bump them piecemeal.",
      "matchFileNames": [
        "ansible/roles/erpnext/**",
        "ansible/roles/services/files/langfuse-compose.yml"
      ],
      "enabled": false
    },
    {
      "description": "Chainguard free-tier images garbage-collect old digests — must stay on :latest, never digest-pinned.",
      "matchPackageNames": ["cgr.dev/chainguard/**"],
      "enabled": false
    },
    {
      "description": "Database & cache engines need a manual data-dir migration on a major bump (see the postgres-18 data-dir gotcha) — allow minor/patch/digest only.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "postgres",
        "mariadb",
        "redis",
        "valkey/valkey",
        "clickhouse/clickhouse-server"
      ],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Ollama ships -rc prereleases (e.g. v0.34.0-rc1) between stables. forge-ai runs the GPU inference the agents depend on -- take stables only.",
      "matchPackageNames": ["ollama/ollama"],
      "ignoreUnstable": true
    },
    {
      "description": "Digest-only refreshes (same tag, re-pushed image) auto-merge — low-risk, still git-visible and not live until an Ansible deploy picks them up.",
      "matchUpdateTypes": ["digest"],
      "automerge": true
    }
  ]
}
