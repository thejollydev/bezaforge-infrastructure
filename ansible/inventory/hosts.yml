---
all:
  children:
    hypervisor_hosts:
      hosts:
        forge-hypervisor:
    docker_hosts:
      hosts:
        forge-ops:
    gpu_hosts:
      hosts:
        forge-ai:
    # forge-brizza retired 2026-08-30 (Brizza v1 torn down whole). The
    # group is kept and left empty so the site.yml play returns by adding
    # a host, not by rebuilding the group. VMID 104 / 10.10.50.20 /
    # forge-brizza.bezaforge.dev are reserved for v2.
    assistant_hosts:
      hosts: {}
    erp_hosts:
      hosts:
        forge-erp:
