---
- name: restart rclone gdrive mount
  ansible.builtin.systemd:
    name: rclone-gdrive-mount.service
    state: restarted
    daemon_reload: true

- name: restart samba
  ansible.builtin.systemd:
    name: smbd
    state: restarted
