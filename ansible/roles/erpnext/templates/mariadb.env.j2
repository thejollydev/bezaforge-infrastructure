# {{ ansible_managed }}
# MariaDB root password for the shared mariadb-database container.
# Consumed by compose.mariadb-shared.yaml (MYSQL_ROOT_PASSWORD=${DB_PASSWORD}).
# Secret comes from ansible-vault: host_vars/forge-erp/vault.yml.
DB_PASSWORD={{ vault_erp_db_password }}
